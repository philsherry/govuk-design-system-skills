---
title: Button component deep dive
version: "1.0.0"
duration: ~90 seconds
---

## What this video shows

What a complete `SKILLS.md` file looks like, using the button component as the example. Covers variants, code examples, macro parameters, accessibility requirements, and do's and don'ts.

## What you will see

1. The frontmatter and overview that open every skills file
2. The five button variants — default, secondary, warning, inverse, start
3. A warning button code example in HTML and Nunjucks
4. The full macro options table
5. Accessibility rules and the do/don't list

## What you will need

- Copy `workspace.example.yaml` to `workspace.yaml`: `cp workspace.example.yaml workspace.yaml`
- Clone `govuk-design-system-skills` into `workspace/`: `npx -y degit philsherry/govuk-design-system-skills workspace/govuk-design-system-skills`

## What comes next

The next video covers the date input component, where prescribed error messages make the skills file essential for getting validation right.
